#给出了一些基本的控件，有(dataList,getObject,iterator,labelEdit,loop,menu,select,setAttribute,tree,write)
#增加了checkBox,dateTimePicker,radio，把所有的基本控件都定义了一个对象，然后重载toString方法，生成控件。
#增加了checkBoxList和radioList
#增加了滚动条
#增加了BI
#去掉BI和滚动条，并且完善功能，使它支持spring的注入。
#生成新的版本。
#增加DB2数据库的支持